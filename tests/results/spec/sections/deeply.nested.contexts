- section 'a'
  - var/escaped 9 'one'
  - lit '\n'
  - section 'b'
    - var/escaped 24 'one'
    - var/escaped 31 'two'
    - var/escaped 38 'one'
    - lit '\n'
    - section 'c'
      - var/escaped 53 'one'
      - var/escaped 60 'two'
      - var/escaped 67 'three'
      - var/escaped 76 'two'
      - var/escaped 83 'one'
      - lit '\n'
      - section 'd'
        - var/escaped 98 'one'
        - var/escaped 105 'two'
        - var/escaped 112 'three'
        - var/escaped 121 'four'
        - var/escaped 129 'three'
        - var/escaped 138 'two'
        - var/escaped 145 'one'
        - lit '\n'
        - section 'e'
          - var/escaped 160 'one'
          - var/escaped 167 'two'
          - var/escaped 174 'three'
          - var/escaped 183 'four'
          - var/escaped 191 'five'
          - var/escaped 199 'four'
          - var/escaped 207 'three'
          - var/escaped 216 'two'
          - var/escaped 223 'one'
          - lit '\n'
        - var/escaped 238 'one'
        - var/escaped 245 'two'
        - var/escaped 252 'three'
        - var/escaped 261 'four'
        - var/escaped 269 'three'
        - var/escaped 278 'two'
        - var/escaped 285 'one'
        - lit '\n'
      - var/escaped 300 'one'
      - var/escaped 307 'two'
      - var/escaped 314 'three'
      - var/escaped 323 'two'
      - var/escaped 330 'one'
      - lit '\n'
    - var/escaped 345 'one'
    - var/escaped 352 'two'
    - var/escaped 359 'one'
    - lit '\n'
  - var/escaped 374 'one'
  - lit '\n'
